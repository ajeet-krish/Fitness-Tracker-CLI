[project]
name = "fitness-tracker-cli"
version = "0.1.0"
description = "Command-line fitness tracking tool built in Python."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
    "scipy>=1.17.1",
]
